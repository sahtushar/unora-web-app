/**
 * Shared row shape for the profile location picker.
 * Results are loaded via Mapbox — see `@/services/locationAutocomplete`.
 */
export interface CitySearchRow {
  id: string;
  label: string;
}
